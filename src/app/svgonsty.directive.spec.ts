import { SvgonstyDirective } from './svgonsty.directive';

describe('SvgonstyDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgonstyDirective();
    expect(directive).toBeTruthy();
  });
});
